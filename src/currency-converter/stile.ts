import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor: "#4129de",
        justifyContent: "center",
        alignItems: "center"
    },
    titolo:{
        fontFamily: "sans",
        fontWeight: "bold",
        fontSize:20,
    },
});